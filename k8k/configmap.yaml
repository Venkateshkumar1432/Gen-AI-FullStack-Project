apiVersion: v1
kind: ConfigMap

metadata:
  name: backend-config
  namespace: production

data:
  NODE_ENV: "production"
  PORT: "5000"
  JWT_SECRET: "12345"
  EMBEDDING_MODEL: "models/gemini-embedding-001"